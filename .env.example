# Firebase (Vite env vars must start with VITE_)
# Copy this file to .env.local and fill in values from your Firebase project settings.

VITE_FIREBASE_API_KEY=
# Usually <project-id>.firebaseapp.com (NOT the Hosting domain *.web.app)
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Optional (only needed if you use Analytics)
VITE_FIREBASE_MEASUREMENT_ID=

# Optional: point withdrawals to the local RazorpayX demo server
# If unset, withdrawals use Supabase Edge Functions.
VITE_WITHDRAWALS_API_URL=

# Optional: Supabase Storage bucket name used for gig cover images.
# Defaults to 'uploads'. If you set a different bucket, ensure it exists and is public
# (or rely on signed download URLs + logged-in users).
VITE_SUPABASE_GIG_IMAGES_BUCKET=

# Optional: force all demo/dummy gigs (the built-in seeded ones) to belong to this seller UID.
# Set this to the Firebase Auth UID of your "amlance" account so orders from demo gigs show
# up in that account's freelancer dashboard.
VITE_DEMO_SELLER_UID=
