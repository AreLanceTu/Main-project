# RazorpayX withdrawals demo server

# Required: Postgres connection string.
# You can use Supabase Postgres (recommended for quick demo) or any local Postgres.
DATABASE_URL=

# Used to sign/verify demo webhook requests (self-call)
RAZORPAYX_WEBHOOK_SECRET=demo_webhook_secret

# Optional
PORT=5055
SELF_BASE_URL=http://localhost:5055
